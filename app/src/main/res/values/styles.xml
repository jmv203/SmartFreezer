<resources>
    <!--Style para EditText -->
    <style name="CustomEditTextStyle">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!--Style para TextView -->
    <style name="CustomTextStyle">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:textSize">16sp</item>
    </style>

    <!--Style para Button -->
    <style name="CustomButtonStyle">
        <item name="android:fontFamily">@font/roboto</item>
        <item name="android:textSize">18sp</item>
    </style>

    <!--Style para PopUpMenu -->
    <style name="PopupMenuPurple" parent="ThemeOverlay.AppCompat.Light">
        <item name="android:popupBackground">@color/purpleButton</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <!--Style para Spinner -->
    <style name="SpinnerThemePurpleText" parent="Theme.AppCompat.Light">
        <item name="android:textColor">@color/purpleButton</item>
    </style>

</resources>
